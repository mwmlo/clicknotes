import React from 'react'
import 'katex/dist/katex.min.css';
import TeX from '@matejmazur/react-katex';

// Import images
import image1 from "../../../images/thermalImg/image1.png"

export default function NoteComponent(props) {
    if (props === 1) {
        return (
            
        )
    } else if (props === 2) {
        return (
            
        )
    } else if (props === 3) {
        return (
            
        )
    } else if (props === 4) {
        return (
            
        )
    } else if (props === 5) {
        return (
            
        )
    }
}
